os: linux
language: cpp
compiler: gcc

before_install:
 - sudo apt-get update -qq
 - sudo apt-get install cmake libboost-all-dev

before_script:
 - mkdir build
 - cd build
 - cmake -DBUILD_TESTS=ON ..

script:
  - make 
  - make test