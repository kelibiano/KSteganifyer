
machine:
  timezone:
    America/Los_Angeles
dependencies:
  pre: sudo apt-get install libboost-dev
checkout:
  post:
    - git submodule sync
    - git submodule update --init
compile:
  override:
    - cmake CMakeLists.txt
    - cmake --build .
      