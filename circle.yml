version: 2
machine:
  timezone: Europe/Dublin
  pre:
    - sudo apt-get install libboost-dev
compile:
  override:
    - cmake CMakeLists.txt
    - cmake --build .
general:
  artifacts:
    - "Steganifyer"
